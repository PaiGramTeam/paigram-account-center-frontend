<template>
  <div class="flex min-h-screen items-center justify-center bg-gray-50">
    <div class="w-full max-w-md rounded-lg bg-white p-8 shadow-md">
      <h1 class="mb-6 text-center text-2xl font-bold">找回密码</h1>
      <a-form :model="form" @submit="handleSubmit">
        <a-form-item field="email" label="邮箱">
          <a-input v-model="form.email" placeholder="请输入注册邮箱" />
        </a-form-item>
        <a-form-item>
          <a-button type="primary" html-type="submit" long :loading="loading">发送重置链接</a-button>
        </a-form-item>
      </a-form>
      <div class="mt-4 text-center text-sm text-gray-600">
        <router-link to="/login" class="text-blue-500">返回登录</router-link>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { Message } from '@arco-design/web-vue'

const loading = ref(false)
const form = reactive({ email: '' })

const handleSubmit = async (): Promise<void> => {
  loading.value = true
  try {
    // TODO: 调用找回密码 API
    Message.success('重置链接已发送到您的邮箱')
  } catch (_error) {
    Message.error('发送失败，请稍后重试')
  } finally {
    loading.value = false
  }
}
</script>
